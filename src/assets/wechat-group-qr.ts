// Base64-encoded WeChat group QR code (~260KB jpeg). Bundled as a module
// so it ships inside main.js — no vault-file dependency, works offline.
export const WECHAT_GROUP_QR_DATA_URL = `data:image/jpeg;base64,`;
